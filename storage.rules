rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow all access for development (we'll secure this later)
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
} 